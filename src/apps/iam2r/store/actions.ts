import { ActionTree } from 'vuex';
import { Commit } from 'vuex';
const actions:ActionTree<any, any> = {
    async login(context: { commit: Commit}, payload: object) {
    
    }
}
export default  actions